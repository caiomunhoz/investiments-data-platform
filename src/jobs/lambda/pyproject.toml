[project]
name = "lambda"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"

dependencies = ["ingestion"]

# entrypoint deployed as-is into the Lambda image, not a library others import
[tool.uv]
package = false