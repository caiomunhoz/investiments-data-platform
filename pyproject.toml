[project]
name = "investiments-data-platform"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["src/etl/*", "src/jobs/*"]

[tool.uv.sources]
ingestion = { workspace = true }

[dependency-groups]
dev = [
    "ruff>=0.16.7",
]
