[project]
name = "investiments-data-platform"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "cryptography>=50.0.1",
    "dlt>=1.30.0",
    "pendulum>=3.2.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.7",
]
